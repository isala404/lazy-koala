apiVersion: v1
kind: ConfigMap
metadata:
 name: gazer-config
 namespace: {{ .Release.Namespace }}
data:
 config.yaml: |
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sherlock-config
  namespace: {{ .Release.Namespace }}
data:
  models.config: |
  services.yaml: |