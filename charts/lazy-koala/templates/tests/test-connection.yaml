apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lazy-koala.fullname" . }}-test-connection"
  labels:
    {{- include "lazy-koala.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lazy-koala.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
